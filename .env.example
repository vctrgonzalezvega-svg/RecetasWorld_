# Puerto del servidor (Render lo asigna automáticamente)
PORT=8081

# Clave maestra para crear usuarios administradores
# En Render, esta se genera automáticamente o se puede configurar manualmente
ADMIN_KEY=tu_clave_secreta_aqui

# Entorno de ejecución
NODE_ENV=production